namespace Services.Models.Enums
{
    public enum ContentType
    {
        // TEMPLATE TYPE (User-selected). AI must NOT choose this.
        ProductShowcase = 1,
        Storytelling = 2,
        EventRecap = 3
    }
}